# API overview

## Backend

The backend exposes routes for running workflows, generating graphs, replaying runs, and managing approvals.

## MCP

The MCP server exposes published workflows as tools so external clients can invoke them by name.
