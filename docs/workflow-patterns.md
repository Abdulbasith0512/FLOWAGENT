# Workflow patterns

## Common patterns

- Trigger -> transform -> LLM -> merge
- Trigger -> condition -> branch
- Trigger -> human approval -> resume

These patterns keep the canvas easy to read and the runtime behavior easy to reason about.
