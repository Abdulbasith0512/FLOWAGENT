name: CI\n\non:\n  push:\n  pull_request:\n\njobs:\n  sanity-check:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Show repository info\n        run: echo 'FlowAgent CI check'\n      - name: Confirm repository structure\n        run: test -d web && test -d backend && test -d mcp\n