name: CI

on:
  push:
  pull_request:

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Show repository info
        run: echo "FlowAgent CI check"